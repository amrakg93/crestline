# ICSE Edutech Frontend — environment variables
# Copy to .env.local for local dev, or set in Vercel dashboard for production

# URL of the deployed Express API (Railway)
NEXT_PUBLIC_API_URL=http://localhost:4000

# URL of the deployed Go graph engine (Railway)
NEXT_PUBLIC_GRAPH_URL=http://localhost:4001
